<resources>
    <!-- Base application theme. -->
    <style name="Theme.FlatAndroid" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/flat_white</item>
        <item name="colorPrimaryVariant">@color/flat_white</item>
        <item name="colorOnPrimary">@color/flat_day_night_text_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/flat_blue</item>
        <item name="colorSecondaryVariant">@color/flat_blue</item>
        <item name="colorOnSecondary">@color/flat_day_night_text_secondary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.FlatAndroid.Launch">
        <item name="android:windowBackground">@drawable/launcher_bg</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:clipToPadding">true</item>
    </style>

    <style name="Theme.FlatAndroid.Fullscreen">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="Theme.FlatAndroid.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.FlatAndroid.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.FlatAndroid.Camera">
        <item name="android:immersive">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

</resources>